OPENAI_API_KEY=sk-...
# Optional: override the API base URL (e.g. an OpenAI-compatible proxy/gateway). Leave blank for the default OpenAI endpoint.
AI_URL=
# Optional: override the default model. Leave blank to use gpt-4o.
AI_MODEL=
